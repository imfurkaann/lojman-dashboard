services:
  app:
    build: .
    container_name: lojman-dashboard
    ports:
      - "0.0.0.0:${HOST_PORT:-3002}:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      APP_URL: ${APP_URL:-http://162.19.242.35:3002}
      SESSION_SECRET: ${SESSION_SECRET:-change_this_to_a_random_secret_key}
      DB_PATH: /data/lojman.db
      TZ: Europe/Istanbul
      LANG: C.UTF-8
      LC_ALL: C.UTF-8
    volumes:
      - ./data:/data
      - ./public/uploads:/app/public/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/dashboard"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 45s

# Using bind mounts to keep DB and uploads on the host filesystem.
# Volumes removed: switched to host folders under the project directory.
